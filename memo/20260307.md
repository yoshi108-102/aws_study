とりあえずlambda agent について学習を進めるぞ
https://docs.aws.amazon.com/lambda/latest/dg/welcome.html

まずはこれを読む


同時並行で、lambdaのhello worldを作らせてみる。

結果
```bash
cloudwatch_log_group = "/aws/lambda/tf-lambda-hello-xxx"
function_url = "https://xxx.lambda-url.ap-northeast-1.on.aws/"
lambda_function_name = "tf-lambda-hello-xxx"
```

と出てきた

```bash
> curl https://xxx.lambda-url.ap-northeast-1.on.aws/
{"message": "Hello from Terraform-managed Lambda", "app_name": "tf-lambda-hello", "method": "GET", "path": "/", "timestamp": "2026-03-07T03:34:44.745345+00:00", "request_id": "xxx"}%
```
GETは成立しているみたい。


```python
import json
import os
from datetime import datetime, timezone


def lambda_handler(event, context):
    request_context = event.get("requestContext") or {}
    http_context = request_context.get("http") or {}

    response_body = {
        "message": "Hello from Terraform-managed Lambda",
        "app_name": os.environ.get("APP_NAME", "unknown-app"),
        "method": http_context.get("method", "UNKNOWN"),
        "path": http_context.get("path", "/"),
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "request_id": getattr(context, "aws_request_id", None),
    }

    return {
        "statusCode": 200,
        "headers": {
            "Content-Type": "application/json"
        },
        "body": json.dumps(response_body, ensure_ascii=False),
    }
```

Python自体は至って普通のコードに見える。
とりあえずGETメソッドとして投げてみたんだけど、他のものはどうなのか？
```bash
> curl -X HEAD -i "https://xxx.lambda-url.ap-northeast-1.on.aws/"
HTTP/1.1 200 OK
Date: Sat, 07 Mar 2026 04:00:26 GMT
Content-Type: application/json
Content-Length: 0
Connection: keep-alive
x-amzn-RequestId: xxx
X-Amzn-Trace-Id: xxx
```

```bash
> curl -X POST \
  -H "Content-Type: application/json" \
  -d '{"name":"yoshi"}' \
    "https://xxx.lambda-url.ap-northeast-1.on.aws/"
{"message": "Hello from Terraform-managed Lambda", "app_name": "tf-lambda-hello", "method": "POST", "path": "/", "timestamp": "2026-03-07T04:01:49.955397+00:00", "request_id": "xxx"}%
```
postもできた。
逆に今のコードでHEADメソッドだけ挙動が違うのってちょっとおかしくないか
methodのところに直接method名を入れてるだけなのに...
よく考えたらResponse.bodyを作ってるだけだから当然か

```bash
 # aws_lambda_function.hello will be updated in-place
  ~ resource "aws_lambda_function" "hello" {
        id                             = "tf-lambda-hello"
      ~ last_modified                  = "2026-03-07T03:33:47.904+0000" -> (known after apply)
      ~ source_code_hash               = "xxx" -> "xxxx"
        tags                           = {}
        # (30 unchanged attributes hidden)

        # (4 unchanged blocks hidden)
    }
```
ところで、terraformのビルドを見たらこんな感じになっているので、ハッシュと最終更新時刻によってコードが管理されているのかな
```bash
terraform state show aws_lambda_function.hello
```
としたら中を読めるらしい

危ないのはマスクした
```json
# aws_lambda_function.hello:
resource "aws_lambda_function" "hello" {
    architectures                  = [
        "x86_64",
    ]
    arn                            = "arn:aws:lambda:ap-northeast-1:xxx:function:tf-lambda-hello-xxx"
    code_sha256                    = "xxx"
    code_signing_config_arn        = null
    description                    = null
    filename                       = "./lambda_function.zip"
    function_name                  = "tf-lambda-hello-xxx"
    handler                        = "lambda_function.lambda_handler"
    id                             = "tf-lambda-hello-xxx"
    image_uri                      = null
    invoke_arn                     = "arn:aws:apigateway:ap-northeast-1:lambda:path/2015-03-31/functions/arn:aws:lambda:ap-northeast-1:xxx:function:tf-lambda-hello-xxx/invocations"
    kms_key_arn                    = null
    last_modified                  = "2026-03-07T04:09:05.000+0000"
    layers                         = []
    memory_size                    = 128
    package_type                   = "Zip"
    publish                        = false
    qualified_arn                  = "arn:aws:lambda:ap-northeast-1:xxx:function:tf-lambda-hello-xxx:$LATEST"
    qualified_invoke_arn           = "arn:aws:apigateway:ap-northeast-1:lambda:path/2015-03-31/functions/arn:aws:lambda:ap-northeast-1:xxx:function:tf-lambda-hello-xxx:$LATEST/invocations"
    region                         = "ap-northeast-1"
    reserved_concurrent_executions = -1
    response_streaming_invoke_arn  = "arn:aws:apigateway:ap-northeast-1:lambda:path/2021-11-15/functions/arn:aws:lambda:ap-northeast-1:xxx:function:tf-lambda-hello-xxx/response-streaming-invocations"
    role                           = "arn:aws:iam::xxx:role/tf-lambda-hello-role-xxx"
    runtime                        = "python3.13"
    signing_job_arn                = null
    signing_profile_version_arn    = null
    skip_destroy                   = false
    source_code_hash               = "xxx"
    source_code_size               = 633
    source_kms_key_arn             = null
    tags                           = {}
    tags_all                       = {}
    timeout                        = 10
    version                        = "$LATEST"

    environment {
        variables = {
            "APP_NAME" = "tf-lambda-hello"
        }
    }

    ephemeral_storage {
        size = 512
    }

    logging_config {
        application_log_level = null
        log_format            = "Text"
        log_group             = "/aws/lambda/tf-lambda-hello-xxx"
        system_log_level      = null
    }

    tracing_config {
        mode = "PassThrough"
    }
}
```