とりあえずlambda agent について学習を進めるぞ
https://docs.aws.amazon.com/lambda/latest/dg/welcome.html

まずはこれを読む


同時並行で、lambdaのhello worldを作らせてみる。

結果
```bash
cloudwatch_log_group = "/aws/lambda/tf-lambda-hello-11b317f4"
function_url = "https://xk4jd5pqz5uhetwwm4zzbkq53q0hqlek.lambda-url.ap-northeast-1.on.aws/"
lambda_function_name = "tf-lambda-hello-11b317f4"
```

と出てきた

```bash
> curl https://xk4jd5pqz5uhetwwm4zzbkq53q0hqlek.lambda-url.ap-northeast-1.on.aws/
{"message": "Hello from Terraform-managed Lambda", "app_name": "tf-lambda-hello", "method": "GET", "path": "/", "timestamp": "2026-03-07T03:34:44.745345+00:00", "request_id": "cedbf338-d027-4f43-a487-9d99ccab516a"}%
```
GETは成立しているみたい。


```python
import json
import os
from datetime import datetime, timezone


def lambda_handler(event, context):
    request_context = event.get("requestContext") or {}
    http_context = request_context.get("http") or {}

    response_body = {
        "message": "Hello from Terraform-managed Lambda",
        "app_name": os.environ.get("APP_NAME", "unknown-app"),
        "method": http_context.get("method", "UNKNOWN"),
        "path": http_context.get("path", "/"),
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "request_id": getattr(context, "aws_request_id", None),
    }

    return {
        "statusCode": 200,
        "headers": {
            "Content-Type": "application/json"
        },
        "body": json.dumps(response_body, ensure_ascii=False),
    }
```

Python自体は至って普通のコードに見える。
とりあえずGETメソッドとして投げてみたんだけど、他のものはどうなのか？
```bash
> curl -I "https://xk4jd5pqz5uhetwwm4zzbkq53q0hqlek.lambda-url.ap-northeast-1.on.aws/"
HTTP/1.1 200 OK
Date: Sat, 07 Mar 2026 04:00:26 GMT
Content-Type: application/json
Content-Length: 0
Connection: keep-alive
x-amzn-RequestId: f9b8cc14-42bb-4e89-a950-85fec1a2fb70
X-Amzn-Trace-Id: Root=1-69aba2d9-5bd56a9a3655b8b978f77953;Parent=3b15e086d7a31cae;Sampled=0;Lineage=1:acdc741f:0
```

```bash
> curl -X POST \
  -H "Content-Type: application/json" \
  -d '{"name":"yoshi"}' \
  "https://xk4jd5pqz5uhetwwm4zzbkq53q0hqlek.lambda-url.ap-northeast-1.on.aws/"
{"message": "Hello from Terraform-managed Lambda", "app_name": "tf-lambda-hello", "method": "POST", "path": "/", "timestamp": "2026-03-07T04:01:49.955397+00:00", "request_id": "7be09ff6-1c88-4b42-917d-e4f6707a525d"}%
```
postもできた。
逆に今のコードでHEADメソッドだけ挙動が違うのってちょっとおかしくないか
methodのところに直接method名を入れてるだけなのに...